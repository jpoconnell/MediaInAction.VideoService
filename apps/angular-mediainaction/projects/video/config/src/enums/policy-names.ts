export const enum eVideoPolicyNames {
  video = 'VideoService.Series',
  detail = 'VideoService.Series',
  setAsShipped = 'VideoService.SetAsShipped',
  setAsCancelled = 'VideoService.SetAsCancelled',
}
